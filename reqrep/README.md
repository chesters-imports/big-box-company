```
=================================================
  BIG BOX COMPANY · REQREP
  CO.BBC-002-RR · request bay · first slice
=================================================
```

**ReqRep** is the Big Box **request bay**: discuss in blocks, Hands **closes AGREED**, lock **scope**, generate **Product prep for an agent**, Hands **signs**. Only signed prep is leave-the-bay law.

Look: **Great Road Mapper family** (IBM Plex, gold accent, BIGBOX chrome) — not pocket cloth, not grey dat admin.

### Run (Deck Host — house path)

**Preferred:** ROM Launcher case **ReqRep** (`reqrep-414b`) — spawns via Deck Host, same as GRM/sopr. Do **not** leave raw `python server.py` running for day work; the host owns the process.

```bat
cd C:\ALICE_BOX\big-box-company\reqrep\prod
run-reqrep.bat
```

- Port **42962** · `run-in-deck-host.py`
- Recipe: `the-deck-host/rom-launcher/.../launches.json` → `reqrep-414b`
- Cat: `dewey-catalog-co/rom-cat/.../catalog.json` · `launcher_show: true`
- Data: `prod/safe_box/cases.json` (local)

Browser-only bat exists for emergencies; Hands observes products **in Deck Host glass**, not Chrome chrome.

### First slice (what ships)

| Surface | Job |
|---------|-----|
| Request list | Title · producer · Hands · priority · status |
| Discussion blocks | Paragraph chunks · thread expand/collapse |
| Speak as Hands / Agent | Post into threads |
| **Close AGREED** | Hands seals a block |
| Scope + lock | Freeze scope text |
| Product prep | Generate (local rewrite) · edit · **Sign (Hands)** |

Design talk: `DESIGN-LOCK.md` · mood mock only: `generic-product-mockup.png`

### Not this slice

LLM rewrite (prep is template from closed blocks), Analytics, multi-tenant, Confluence cosplay bulk.
